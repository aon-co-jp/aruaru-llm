# PORTING.md — راهنمای پورت کردن aruaru-llm (نسخه‌ی فشرده)

> **توجه**: این یک ترجمه‌ی فشرده است. راهنمای فنی کامل با جزئیات کد
> و نکات دام‌گونه تنها به ژاپنی در [PORTING.md](PORTING.md) در دسترس
> است — پیش از پذیرش واقعی یک الگو به آنجا مراجعه کنید.

خلاصه‌ای از الگوهای پیاده‌سازیِ قابل‌استفاده‌ی مجدد این پروژه، برای
زمانی که به پروژه‌ی دیگری منتقل شوند:

1. **الگوی اتصال با open-cuda (پیکربندی SET)**: وابستگی مسیری به
   `opencuda-core`/`opencuda-cpu`؛ اجرای واقعی هسته‌ی GPU را فراخوانی
   می‌کند (`alloc_buffer`→`copy_from_host`→`launch_kernel`→
   `synchronize`→`copy_to_host`).
2. **طبقه‌بندی قصد مبتنی بر قاعده، طراحی‌شده برای جایگزینی آینده با
   LLM واقعی**: فیلد `engine` را حفظ کنید و همیشه در آن به‌طور
   صادقانه اعلام کنید کدام پیاده‌سازی واقعاً استفاده شده است.
3. **لایه‌ی API از طریق RPoem** (`open-runo-poem-compat`) به‌جای
   crate واقعی `poem` — بدون استخراج‌کننده‌ی `Data<T>`، وضعیت مشترک
   از طریق closure و `Arc::clone` گرفته می‌شود.
4. **الگوی اعتبارسنجی ورودی خالی** (2026-08-06): `400 Bad Request`
   صریح به‌جای اجازه دادن به نشت خطاهای داخلی توکنایزر به‌صورت `503`
   گمراه‌کننده.
5. **الگوی ثبت مستأجرِ «شبیه‌سازِ سایه»** (مشترک با
   `open-web-server`): `TenantRegistry` + نقاط پایانی `/admin/tenants`.
6. **قابلیت تولید واقعی از طریق `opencuda-llm::GptModel`**: وزن‌های
   GPT-2 124M — فیلد `disclosure` هرگز نباید حذف شود، `/v1/chat` و
   `/v1/generate` نباید ادغام شوند.
7. **تشخیص سخت‌افزار ← اندازه‌ی LLM پیشنهادی ← دانلود خودکار**
   (فیچرهای اختیاری `hw-detect-vulkan`/`hw-detect-directx`، الگوی
   بررسی متقابل، افشای صادقانه‌ی اینکه این تنها یک هیوریستیک تقریبی
   اندازه در برابر VRAM است).
8. **الگوی افزونه‌ی ترجمه** (فیچر `nllb-translate`): یک وابستگی
   سنگین اختیاری `rust-bert`/`tch`، جداشده پشت یک فیچر Cargo، به‌طور
   پیش‌فرض غیرفعال.
9. **فیچر dispatch مربوط به `real-vulkan`** — **توجه**: هنوز برای
   پورت به جای دیگر توصیه نمی‌شود، به دلیل باگ شناخته‌شده و
   حل‌نشده (`Linear::forward` بایت‌های SPIR-V مربوط به `matmul.spv`
   را به `sgemm` متصل نمی‌کند، که باعث شکست فوری
   `GemmPath::VulkanGeneric` می‌شود).
10. **الگوی جریمه‌ی تکرار** (`generate_with_repetition_penalty`،
    پیش‌فرض `1.3`، قابل بازنویسی از طریق متغیر محیطی).

**هشدار مهم**: GPT-2 124M کوچک و متعلق به سال 2019 است — با LLMهای
تجاری مدرن قابل مقایسه نیست. `/v1/chat` همچنان مبتنی بر قاعده +
طبقه‌بندی شباهت مبتنی بر رمزگذار باقی می‌ماند، نه تولید گفت‌وگوی
عصبی. این نکته باید در هر مقصد پورت نیز افشا شود.

---

زبان‌های دیگر: [日本語 (اصلی، جزئیات کامل)](PORTING.md) ·
[Deutsch](PORTING-German.md) · [Italiano](PORTING-Italian.md) ·
[Français](PORTING-French.md) · [Русский](PORTING-Russian.md) ·
[Українська](PORTING-Ukrainian.md) · [עברית](PORTING-Hebrew.md) · [العربية](PORTING-Arabic.md)
