# PORTING.md — دليل نقل aruaru-llm (نسخة مختصرة)

> **ملاحظة**: هذه ترجمة مختصرة. يظل الدليل التقني الكامل بتفاصيل
> الشيفرة والمزالق متاحاً باليابانية فقط في [PORTING.md](PORTING.md)
> — راجعه قبل تبنّي أي نمط فعلياً.

ملخص لأنماط التنفيذ القابلة لإعادة الاستخدام من هذا المشروع، في حال
نقلها إلى مشروع آخر:

1. **نمط الاقتران مع open-cuda (تهيئة SET)**: اعتماد مساري على
   `opencuda-core`/`opencuda-cpu`؛ يستدعي تنفيذاً حقيقياً لنواة GPU
   (`alloc_buffer`←`copy_from_host`←`launch_kernel`←`synchronize`←
   `copy_to_host`).
2. **تصنيف نوايا قائم على القواعد، مصمَّم لاستبدال مستقبلي بنموذج LLM
   حقيقي**: الاحتفاظ بحقل `engine` والإفصاح فيه دائماً بصدق عن التنفيذ
   المستخدَم فعلياً.
3. **طبقة API عبر HTTP باستخدام RPoem** (`open-runo-poem-compat`)
   بدلاً من حزمة `poem` الحقيقية — لا يوجد مستخرج `Data<T>`، تُلتقط
   الحالة المشتركة عبر closure و`Arc::clone`.
4. **نمط التحقق من المدخلات الفارغة** (2026-08-06): `400 Bad Request`
   صريح بدلاً من السماح بتسرب أخطاء المُرمِّز الداخلية كخطأ `503`
   مضلِّل.
5. **نمط تسجيل المستأجرين «الاستنساخ الظلي»** (مشترك مع
   `open-web-server`): `TenantRegistry` + نقاط النهاية
   `/admin/tenants`.
6. **قدرة توليد حقيقية عبر `opencuda-llm::GptModel`**: أوزان
   GPT-2 124M — يجب ألا يُحذف حقل `disclosure` أبداً، ويجب عدم دمج
   `/v1/chat` و`/v1/generate`.
7. **اكتشاف العتاد ← حجم LLM موصى به ← تنزيل تلقائي** (ميزتان
   اختياريتان `hw-detect-vulkan`/`hw-detect-directx`، نمط تحقق
   متقاطع، إفصاح صادق بأن هذا مجرد قاعدة تقريبية للحجم مقابل VRAM).
8. **نمط إضافة الترجمة** (ميزة `nllb-translate`): اعتماد ثقيل اختياري
   على `rust-bert`/`tch`، معزول خلف ميزة Cargo، معطّل افتراضياً.
9. **ميزة إرسال `real-vulkan`** — **ملاحظة**: لا يُنصح بعد بنقلها إلى
   مكان آخر، بسبب خلل معروف غير محلول (`Linear::forward` لا يربط
   بايتات SPIR-V الخاصة بـ`matmul.spv` بـ`sgemm`، مما يجعل
   `GemmPath::VulkanGeneric` يفشل فوراً).
10. **نمط عقوبة التكرار** (`generate_with_repetition_penalty`،
    الافتراضي `1.3`، قابل للتجاوز عبر متغير بيئة).

**تحذير مهم**: GPT-2 124M نموذج صغير من عام 2019 — لا يُقارَن بنماذج
اللغة التجارية الحديثة. يبقى `/v1/chat` قائماً على القواعد + تصنيف
التشابه عبر المُرمِّز، وليس توليد حوار عصبي. يجب الإفصاح عن هذا أيضاً
في كل وجهة نقل.

---

لغات أخرى: [日本語 (الأصل، تفاصيل كاملة)](PORTING.md) ·
[Deutsch](PORTING-German.md) · [Italiano](PORTING-Italian.md) ·
[Français](PORTING-French.md) · [Русский](PORTING-Russian.md) ·
[Українська](PORTING-Ukrainian.md) · [עברית](PORTING-Hebrew.md) · [فارسی](PORTING-Persian.md)
